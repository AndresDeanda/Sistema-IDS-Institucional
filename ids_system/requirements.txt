# =============================================================
#   SISTEMA IDS INSTITUCIONAL - Dependencias Python
#   Instalación: pip install -r requirements.txt
# =============================================================

# Captura de paquetes de red (requiere Npcap en Windows)
scapy==2.5.0

# Carga de variables de entorno desde .env
python-dotenv==1.0.1
